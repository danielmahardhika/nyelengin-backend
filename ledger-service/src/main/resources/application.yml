server:
  port: 8080

spring:
  application:
    name: ledger-service

  datasource:
    url: jdbc:postgresql://localhost:5432/nyelengindb
    username: root
    password: password123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: NyelenginSuperSecretKey_MinimalTigaPuluhDuaKarakter!
